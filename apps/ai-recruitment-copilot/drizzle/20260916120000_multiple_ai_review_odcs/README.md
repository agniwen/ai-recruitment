# 多人 ODC：新旧版本共存

本次是增量结构迁移，必须先执行，再启动依赖关联表的新代码。
它只新增关联表、索引和兼容触发器，不删除旧字段，也不搬迁历史指派数据。

## 共存期

- 保留 `studio_interview.ai_review_assigned_odc_user_id`，线上旧代码继续按原样读写。
- 新代码在原有工作区、来源和角色限制内，允许旧字段或关联表任一命中的 ODC 查看。
- 新代码审批时，全部所选 ODC 只写入关联表，不回写任何 ODC ID 到旧字段。
  同时清空该候选人的旧指派字段，避免历史人员通过兼容查询保留过期权限。
  旧版 ODC 列表不识别这些新指派；这是共存期间的预期行为，需要通过新版列表验证。
- 旧代码重新审批或重新激活时，触发器清理该候选人过期的多人指派。
  新代码在同一事务内清空旧指派字段，再重建关联表记录。
- 删除旧字段指向的用户时，保留其他 ODC 的指派。
- TG 独立评估链接的权限和评估流程不变。

## 后续清理（本次不执行）

1. 确认所有应用实例和写入任务都已使用新版，旧代码不再写入旧字段。
2. 通过后续数据迁移，把仍仅保存在旧字段里的有效指派补入关联表，按候选人和用户去重。
3. 发布仅使用关联表的代码，移除兼容查询及旧字段清理逻辑；保留数据库旧字段，直到所有旧实例退出。
4. 再通过独立结构迁移删除兼容触发器、函数和旧字段。

本地与线上共用数据库时，运行本地新版也会修改共享数据并可能发送真实 TG 通知。
迁移与功能测试应区分执行；本目录的迁移文件不会自动执行。
